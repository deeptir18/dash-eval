#!/bin/bash
SERVER=$1
SERVER_MOUNT=$2
#rm_command="rm -rf ${SERVER_MOUNT}/dash > /dev/null 2>&1"
#ssh ${USERNAME}@${SERVER} -i ${KEY} $rm_command
#mkdir_command="mkdir ${SERVER_MOUNT}/dash/"
#ssh ${USERNAME}@${SERVER} -i ${KEY} $mkdir_command
#cp_command="cp -r ${SERVER_MOUNT}/logs ${SERVER_MOUNT}/dash"
#ssh ${USERNAME}@${SERVER} -i ${KEY} $cp_command
